import React from "react";

const sub = props => {
    return (
        <div className="sub_header">
            <p className="sub_header_p">{props.label}</p>
        </div>
    );
};

export default sub;
